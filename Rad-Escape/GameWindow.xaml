<Window x:Class="Rad_Escape.GameWindowClass"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Rad_Escape"
        mc:Ignorable="d"
        Title="GameWindow" Height="900" Width="1600" Closing="Window_Closing">
    <Grid>

        <Viewbox Stretch="UniformToFill">
            <Image x:Name="BackgroundImage" Source="{Binding BackgroundBitmapImage}" />
        </Viewbox>
        <DockPanel>
            <Grid DockPanel.Dock="Top">
                <StackPanel Orientation="Horizontal" MinHeight="100" MaxHeight="150" HorizontalAlignment="Right" Margin="10">
                </StackPanel>
                <Separator VerticalAlignment="Bottom" />
            </Grid>
            <StackPanel VerticalAlignment="Center">
                <Grid Background="Black">
                    <Viewbox MaxHeight="300">
                        <Label x:Name="TimerLabel" Content="{Binding Path=CurrentText}" Foreground="GhostWhite" HorizontalAlignment="Center" />
                    </Viewbox>
                </Grid>
            </StackPanel>
        </DockPanel>
        <Frame x:Name="DisplayFrame" />
    </Grid>
</Window>